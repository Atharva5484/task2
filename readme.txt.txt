# Library RESTful API with HTML Frontend

This project is a simple RESTful API built using Python and Flask for a library system. It includes CRUD operations and a browser-based frontend to view and search books.

## Features
- GET, POST, PUT, DELETE for books
- JSON-based API
- Pretty HTML frontend with search box
- No database — uses in-memory list

## Setup

```bash
pip install flask
python app.py
